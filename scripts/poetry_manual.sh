curl -sSL https://install.python-poetry.org | POETRY_HOME=/opt/environments/poetry python3 -


cat " After successful completion, logout and run this to enable bash completions ` poetry completions bash >> /etc/profile.d/bash_completion.sh `"

